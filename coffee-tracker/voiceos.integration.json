{
  "schemaVersion": 1,
  "id": "com.you.coffee-tracker",
  "version": "1.1.0",
  "name": "Coffee Tracker",
  "summary": "Log every coffee by voice and glance your habits in the notch.",
  "publisher": { "id": "pub_you", "name": "You" },
  "runtime": { "kind": "local-mcp", "command": "bun", "args": ["server.ts"] },
  "tools": [
    {
      "name": "log_coffee",
      "title": "Log coffee",
      "description": "Log coffee the user drank. Use when the user says they had, drank, or want to log coffee. Call once per request — use count for multiple coffees, never repeated calls.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "drink": { "type": "string", "description": "What they drank, e.g. flat white" },
          "shots": { "type": "number", "description": "Espresso shots per coffee (default 1)" },
          "count": { "type": "number", "description": "How many of this coffee to log (default 1)" }
        },
        "required": ["drink"]
      },
      "confirmation": {
        "schemaVersion": 1,
        "root": {
          "type": "card",
          "title": "Log coffee",
          "children": [
            { "type": "textField", "bind": "{{drink}}", "label": "Drink" },
            { "type": "textField", "bind": "{{count}}", "label": "How many" }
          ],
          "footer": [
            {
              "type": "actions",
              "items": [
                { "label": "Cancel", "role": "cancel" },
                { "label": "Log it", "role": "confirm", "color": "accent" }
              ]
            }
          ]
        }
      }
    },
    {
      "name": "remove_coffee",
      "title": "Remove coffee",
      "description": "Remove mistakenly logged coffees, most recent first. Use when the user says the log is wrong, too many were logged, or they want to undo, delete, or correct logged coffee.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "count": { "type": "number", "description": "How many recent entries to remove (default 1)" },
          "drink": { "type": "string", "description": "Only remove entries matching this drink, e.g. flat white" }
        }
      },
      "confirmation": {
        "schemaVersion": 1,
        "root": {
          "type": "card",
          "title": "Remove coffee",
          "children": [
            { "type": "textField", "bind": "{{count}}", "label": "How many to remove" },
            { "type": "textField", "bind": "{{drink}}", "label": "Only this drink (optional)" }
          ],
          "footer": [
            {
              "type": "actions",
              "items": [
                { "label": "Cancel", "role": "cancel" },
                { "label": "Remove", "role": "confirm", "color": "accent" }
              ]
            }
          ]
        }
      }
    },
    {
      "name": "coffee_stats",
      "title": "Coffee stats",
      "description": "Show how much coffee the user has been drinking. Use when the user asks about their coffee habits, count, or stats.",
      "inputSchema": { "type": "object", "properties": {} }
    }
  ]
}
